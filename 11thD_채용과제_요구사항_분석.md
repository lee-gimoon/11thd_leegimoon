# 11thD React + Spring Boot 풀스택 개발자 채용 과제 분석

> 과제명: **Project Collab — 프로젝트 협업 서비스 구현**  
> 제출 기한: **2026년 8월 26일(수) 23:59**  
> 제출 방법: 안내 메일에 **공개 GitHub Repository 링크**로 회신  
> 현재 기준: 2026년 8월 24일, **백엔드 완성과 README 품질을 최우선**으로 진행

---

## 1. 한눈에 보는 결론

### 프로젝트 목표

사용자가 프로젝트를 만들고, 프로젝트별 멤버 역할(`OWNER`, `ADMIN`, `MEMBER`)에 따라 작업을 함께 관리하는 **백엔드 중심의 프로젝트 협업 서비스**를 구현한다.

이 과제는 단순 CRUD 구현 여부보다 다음 네 가지를 중점적으로 확인하는 과제다.

1. **도메인 설계**: 사용자·프로젝트·멤버십·작업의 관계를 자연스럽게 모델링했는가?
2. **권한과 데이터 격리**: 프로젝트 역할에 따라 기능을 제한하고, 비멤버에게 데이터가 노출되지 않는가?
3. **동시성 제어**: 같은 작업을 동시에 수정할 때 덮어쓰기를 감지하고 명확하게 처리하는가?
4. **설계 설명 능력**: 정해지지 않은 요구사항을 합리적으로 결정하고 README에서 근거를 설명하는가?

### 구현 순서

1. 프로젝트 실행 환경 구성  
   Java 17, Spring Boot 3.3.x, Gradle, H2, JPA, Swagger 설정
2. 도메인 설계  
   `User → Project → ProjectMember → Task` 순으로 엔티티와 관계, 제약조건 구현
3. 기본 CRUD와 권한·데이터 격리 동시 구현  
   API를 만든 뒤 권한을 나중에 붙이지 말고, 서비스 메서드마다 처음부터 권한 검사를 포함합니다.
4. 작업 목록 기능  
   프로젝트 범위 내 검색, 상태 필터, 페이징 구현
5. 동시성 제어  
   `@Version`과 요청 버전을 이용해 오래된 수정 요청을 `409 Conflict`로 처리
6. 테스트 및 Swagger 검증  
   특히 비멤버 접근, 마지막 OWNER 보호, 담당자 권한, 동시 수정 충돌 테스트
7. README 작성  
   구현하면서 내린 결정을 바로 기록하고 마지막에 실행·검증 절차까지 완성

### 필수 구현 핵심

- **사용자**: 등록, 조회
- **프로젝트**: CRUD, 내가 속한 프로젝트 목록 조회
- **멤버**: 추가, 역할 변경, 제거, 목록 조회
- **작업**: CRUD, 검색, 상태 필터, 페이징
- **권한**: 역할별 접근 제한, 마지막 OWNER 보호, 비멤버 데이터 노출 차단
- **동시성**: 동일 작업 수정 충돌 감지 및 `409 Conflict` 처리
- **실행·문서화**: `./gradlew bootRun`, Swagger UI, 실행 절차와 설계 근거를 담은 README

프론트엔드는 선택 사항이며 미구현해도 감점이 없다. 제한된 시간에는 **백엔드 핵심 기능 → 테스트 → Swagger/초기 데이터 → README → 선택 기능** 순서가 가장 안전하다.

---

## 2. 반드시 지켜야 하는 제출 조건

### 실행 및 기술 조건

- Java 17
- Spring Boot 3.3.x
- Gradle 8.x 및 **Gradle Wrapper 포함**
- JPA(Hibernate)
- H2 인메모리 데이터베이스
- OpenAPI 3 및 Swagger UI
- 클론 후 `./gradlew bootRun` 한 번으로 실행 가능해야 함
- 별도 DB, Docker, 환경 변수, 외부 서비스 없이 기본 실행 가능해야 함
- API 키·비밀번호 등 민감 정보 저장소 커밋 금지

### 제출물 조건

- 공개 GitHub Repository
- 실행 방법과 Swagger UI 경로가 포함된 `README.md`
- 실행 직후 기능을 확인할 수 있는 절차
- REST API 요약
- 주요 설계 결정과 이유
- 도입 기술과 선택 이유
- 회사별 데이터 완전 분리, 즉 멀티테넌시 확장 방안
- 미완성 항목과 추가 시간이 있을 때의 진행 계획

---

## 3. 요구사항 분류

### 3.1 필수 도메인

| 도메인 | 필수 내용 | 설계상 핵심 |
| :-- | :-- | :-- |
| 사용자 | 등록, 조회 | 인증 대상이 아니며 요청자 ID를 API 파라미터로 전달 |
| 프로젝트 | 생성, 조회, 수정, 삭제 | 생성자는 자동으로 `OWNER`가 됨 |
| 프로젝트 멤버 | 추가, 역할 변경, 제거, 목록 조회 | 사용자와 프로젝트의 N:M 관계 및 프로젝트별 역할 표현 |
| 작업 | 생성, 조회, 수정, 삭제 | 프로젝트 소속, 제목, 담당자, 상태, 동시 수정 대응 |

`ProjectMember`는 단순 연결 테이블이 아니라 `role`을 가진 독립적인 도메인으로 두는 것이 적절하다.

### 3.2 필수 API 기능

#### 사용자

- 사용자 등록
- 사용자 단건 조회

#### 프로젝트

- 프로젝트 생성
- 프로젝트 단건 조회
- 프로젝트 수정
- 프로젝트 삭제
- 요청자가 속한 프로젝트 목록 조회
- 프로젝트 멤버 추가
- 멤버 역할 변경
- 멤버 제거
- 프로젝트 멤버 목록 조회

#### 작업

- 작업 생성
- 작업 단건 조회
- 작업 수정
- 작업 삭제
- 작업 목록 조회
  - 검색
  - 상태 필터
  - 페이징
- 동일 작업 동시 수정 충돌 처리

### 3.3 선택 사항

- React 화면
- QueryDSL, MapStruct, Redis, Kafka, Docker, CI 등 부가 기술
- 테스트는 안내서상 선택이지만, 이 과제의 핵심 규칙을 입증하려면 사실상 작성하는 편이 좋음

선택 기술은 많이 쓰는 것이 장점이 아니다. 과제 규모에서 필요성이 분명하지 않은 기술은 넣지 않는 편이 더 설득력 있다.

---

## 4. 권한 규칙 정리

### 명세에 명시된 규칙

| 기능 | OWNER | ADMIN | MEMBER | 비멤버 |
| :-- | :--: | :--: | :--: | :--: |
| 프로젝트 생성 | 가능 | 가능 | 가능 | 가능 |
| 프로젝트 상세 조회 | 가능 | 가능 | 가능 | 불가 |
| 프로젝트 수정 | 가능 | 가능 | 불가 | 불가 |
| 프로젝트 삭제 | 가능 | 불가 | 불가 | 불가 |
| 멤버 목록 조회 | 가능 | 가능 | 가능 | 불가 |
| 멤버 추가·역할 변경·제거 | 가능 | 가능 | 불가 | 불가 |
| 작업 생성·조회·목록 조회 | 가능 | 가능 | 가능 | 불가 |
| 작업 수정·삭제 | 가능 | 가능 | 담당자인 경우만 가능 | 불가 |

추가 불변 조건:

- 프로젝트에는 **항상 최소 한 명의 OWNER**가 있어야 한다.
- 비멤버는 조회를 포함해 해당 프로젝트와 작업에 아무 작업도 할 수 없다.
- 다른 프로젝트의 데이터가 목록 또는 상세 응답에 섞여서는 안 된다.

### 구현 시 빠뜨리기 쉬운 부분

- 프로젝트 ID만 알면 상세 조회가 되는 보안 누수
- 전체 작업 조회 후 애플리케이션에서 필터링해 다른 프로젝트 데이터가 잠시라도 조회되는 구조
- 마지막 OWNER를 `ADMIN` 또는 `MEMBER`로 강등
- 마지막 OWNER 멤버 제거
- 다른 프로젝트 사용자를 작업 담당자로 지정
- `MEMBER`가 자신이 담당하지 않은 작업을 수정 또는 삭제
- 프로젝트를 수정할 수 있다는 이유만으로 `ADMIN`에게 프로젝트 삭제까지 허용

권한 검사는 컨트롤러마다 흩어 놓기보다 서비스 계층에서 일관되게 적용하는 편이 좋다. 특히 조회 쿼리 자체에 `projectId` 또는 멤버십 조건을 포함해 데이터 범위를 제한해야 한다.

---

## 5. 명세에 없으므로 직접 결정해야 하는 항목

아래 항목에는 유일한 정답이 없다. 중요한 것은 **일관된 구현과 README의 판단 근거**다.

| 결정 대상 | 권장 결정 | 이유 |
| :-- | :-- | :-- |
| 요청자 식별자 전달 | 공통 요청 헤더 `X-Requester-Id` | 모든 리소스에 반복되는 횡단 관심사이며 URL 자원 식별과 분리하기 쉬움 |
| 작업 상태 | `TODO`, `IN_PROGRESS`, `DONE` | 과제 범위에 충분하고 설명하기 쉬움 |
| 작업 검색 | 제목의 대소문자 무시 부분 일치 | 사용자가 예상하기 쉽고 구현 복잡도가 낮음 |
| 기본 페이징 | `page=0`, `size=20`, 생성일 또는 ID 내림차순 | 응답 크기를 제한하고 결과 순서를 안정적으로 유지 |
| 담당자 유효성 | 같은 프로젝트의 현재 멤버만 지정 가능 | 프로젝트 데이터 경계를 유지 |
| 담당자 필수 여부 | 미지정 허용 | 백로그 작업 생성과 멤버 제거 상황을 단순하게 처리 가능 |
| 멤버 제거 시 담당 작업 | 담당자를 `null`로 변경 후 제거 | 기존 작업 이력을 보존하면서 탈퇴한 사용자의 권한을 제거 |
| 중복 멤버 추가 | `409 Conflict` | 현재 상태와 요청이 충돌한다는 의미를 명확히 표현 |
| 마지막 OWNER 변경·제거 | `409 Conflict` | 요청 형식보다 도메인 불변 조건과 충돌하는 문제 |
| 미존재 리소스 | `404 Not Found` | 일반적인 REST 의미에 부합 |
| 권한 부족 | `403 Forbidden` | 요청자 식별은 되었지만 해당 동작 권한이 없음 |
| 입력값 오류 | `400 Bad Request` | Bean Validation과 통일된 오류 응답 사용 |

### 역할 변경에서 추가로 정할 정책

명세대로라면 `ADMIN`도 멤버 역할을 변경할 수 있지만, `ADMIN`이 `OWNER`를 강등하거나 자신을 `OWNER`로 승격할 수 있는지는 정해져 있지 않다.

권장 정책:

- `OWNER` 역할의 부여·변경·제거는 `OWNER`만 가능
- `ADMIN`은 `ADMIN`과 `MEMBER` 범위에서만 멤버를 관리
- 누구도 마지막 `OWNER`를 강등하거나 제거할 수 없음

이 정책은 명세의 최소 권한 규칙을 더 안전하게 구체화한 결정이므로 README에 별도로 명시해야 한다.

---

## 6. 권장 도메인 모델

```text
User 1 ── N ProjectMember N ── 1 Project
                                  │
                                  └── 1 ── N Task

Task N ── 0..1 User (assignee)
```

### 엔티티별 핵심 필드 예시

#### User

- `id`
- `name`
- `email` — 중복 금지 권장
- `createdAt`, `updatedAt`

#### Project

- `id`
- `name`
- `description`
- `createdAt`, `updatedAt`

#### ProjectMember

- `id`
- `projectId`
- `userId`
- `role` — `OWNER`, `ADMIN`, `MEMBER`
- `createdAt`, `updatedAt`
- `(project_id, user_id)` 유니크 제약

#### Task

- `id`
- `projectId`
- `title`
- `description`
- `status` — `TODO`, `IN_PROGRESS`, `DONE`
- `assigneeId` — nullable 권장
- `version` — 낙관적 락용
- `createdAt`, `updatedAt`

엔티티를 API 응답으로 직접 노출하지 말고 요청·응답 DTO를 분리하는 편이 좋다. 지연 로딩, 순환 참조, 내부 필드 노출 문제를 피하고 API 계약을 명확하게 만들 수 있다.

---

## 7. 동시 수정 처리 권장안

### 선택: 낙관적 락

작업 엔티티에 JPA `@Version` 필드를 두고 클라이언트가 조회한 `version`을 수정 요청에 함께 전송한다.

```json
{
  "title": "API 문서 작성",
  "description": "Swagger 예시 추가",
  "status": "IN_PROGRESS",
  "assigneeId": 2,
  "version": 3
}
```

처리 흐름:

1. 두 사용자가 동일한 작업의 `version=3` 데이터를 조회한다.
2. 첫 번째 사용자가 수정에 성공하면 DB 버전이 `4`가 된다.
3. 두 번째 사용자가 `version=3`으로 수정하면 충돌로 거절한다.
4. API는 `409 Conflict`와 현재 데이터를 다시 조회하라는 메시지를 반환한다.

주의할 점:

- 엔티티에 `@Version`만 추가하고 클라이언트가 본 버전을 전달하지 않으면 순차적으로 도착한 오래된 수정 요청을 확실히 구분하기 어렵다.
- 요청 버전과 현재 버전을 비교하되, 비교 이후 실제 커밋 사이의 경쟁 상태는 JPA 낙관적 락이 최종 방어해야 한다.
- `ObjectOptimisticLockingFailureException` 등을 전역 예외 처리에서 `409 Conflict`로 변환한다.

비관적 락은 충돌 가능성이 높고 반드시 직렬화해야 할 때 적합하지만, 일반적인 협업 작업 수정에는 잠금 비용이 적고 충돌을 사용자에게 알릴 수 있는 낙관적 락이 더 자연스럽다.

---

## 8. REST API 초안

아래는 구현 방향을 잡기 위한 예시다. 실제 구현 후 README에는 확정된 경로와 스키마를 기록한다.

### 사용자

| Method | Path | 설명 |
| :-- | :-- | :-- |
| `POST` | `/api/users` | 사용자 등록 |
| `GET` | `/api/users/{userId}` | 사용자 조회 |

### 프로젝트

| Method | Path | 권한/설명 |
| :-- | :-- | :-- |
| `POST` | `/api/projects` | 누구나 생성, 요청자가 OWNER가 됨 |
| `GET` | `/api/projects` | 요청자가 속한 프로젝트 목록 |
| `GET` | `/api/projects/{projectId}` | 프로젝트 멤버 |
| `PATCH` | `/api/projects/{projectId}` | OWNER, ADMIN |
| `DELETE` | `/api/projects/{projectId}` | OWNER |

### 프로젝트 멤버

| Method | Path | 권한/설명 |
| :-- | :-- | :-- |
| `GET` | `/api/projects/{projectId}/members` | 프로젝트 멤버 |
| `POST` | `/api/projects/{projectId}/members` | OWNER, ADMIN |
| `PATCH` | `/api/projects/{projectId}/members/{userId}` | OWNER, ADMIN 및 추가 정책 적용 |
| `DELETE` | `/api/projects/{projectId}/members/{userId}` | OWNER, ADMIN 및 마지막 OWNER 보호 |

### 작업

| Method | Path | 권한/설명 |
| :-- | :-- | :-- |
| `POST` | `/api/projects/{projectId}/tasks` | 프로젝트 멤버 |
| `GET` | `/api/projects/{projectId}/tasks` | 프로젝트 멤버, 검색·상태·페이징 |
| `GET` | `/api/projects/{projectId}/tasks/{taskId}` | 프로젝트 멤버 |
| `PATCH` | `/api/projects/{projectId}/tasks/{taskId}` | 담당자 또는 OWNER, ADMIN |
| `DELETE` | `/api/projects/{projectId}/tasks/{taskId}` | 담당자 또는 OWNER, ADMIN |

목록 조회 예시:

```http
GET /api/projects/1/tasks?keyword=문서&status=IN_PROGRESS&page=0&size=20
X-Requester-Id: 2
```

`taskId`만으로 작업을 조회하지 않고 `projectId`와 `taskId`를 함께 조건으로 사용하면 프로젝트 경계를 코드와 쿼리 양쪽에서 드러낼 수 있다.

---

## 9. 오류 응답 권장 형식

오류 형식을 하나로 통일하면 Swagger 사용성과 테스트 가독성이 좋아진다.

```json
{
  "code": "TASK_VERSION_CONFLICT",
  "message": "다른 사용자가 먼저 작업을 수정했습니다. 최신 정보를 다시 조회해 주세요.",
  "status": 409,
  "timestamp": "2026-08-24T15:30:00+09:00",
  "path": "/api/projects/1/tasks/10"
}
```

권장 코드 예시:

- `USER_NOT_FOUND`
- `PROJECT_NOT_FOUND`
- `TASK_NOT_FOUND`
- `PROJECT_ACCESS_DENIED`
- `TASK_ACCESS_DENIED`
- `MEMBER_ALREADY_EXISTS`
- `ASSIGNEE_NOT_PROJECT_MEMBER`
- `LAST_OWNER_CANNOT_BE_REMOVED`
- `TASK_VERSION_CONFLICT`

보안을 더 엄격하게 보고 싶다면 비멤버가 프로젝트 존재 여부를 추측하지 못하도록 일부 조회 요청에 `404`를 반환하는 정책도 가능하다. 다만 명세상 권한 부족을 일관되게 표현하려면 `403`을 사용해도 충분하며, 선택한 정책을 README에 적으면 된다.

---

## 10. 권장 패키지 및 계층 구성

과제 규모에서는 도메인별 패키지와 단순한 계층 구조면 충분하다.

```text
com.example.projectcollab
├── common
│   ├── config
│   ├── exception
│   └── response
├── user
│   ├── controller
│   ├── service
│   ├── repository
│   ├── domain
│   └── dto
├── project
│   ├── controller
│   ├── service
│   ├── repository
│   ├── domain
│   └── dto
└── task
    ├── controller
    ├── service
    ├── repository
    ├── domain
    └── dto
```

과제 시간과 규모를 고려하면 마이크로서비스, 이벤트 브로커, Redis, 복잡한 헥사고날 계층 등은 명확한 필요가 없는 한 피하는 편이 좋다.

---

## 11. 테스트 우선순위

모든 CRUD 성공 케이스를 반복하기보다 평가 포인트가 드러나는 규칙을 먼저 테스트한다.

### 반드시 권장하는 테스트

- 프로젝트 생성자가 `OWNER`로 등록된다.
- 비멤버는 프로젝트와 작업을 조회할 수 없다.
- `ADMIN`은 프로젝트를 수정할 수 있지만 삭제할 수 없다.
- `MEMBER`는 자신이 담당한 작업만 수정·삭제할 수 있다.
- 다른 프로젝트 멤버를 작업 담당자로 지정할 수 없다.
- 마지막 `OWNER`를 강등하거나 제거할 수 없다.
- 작업 목록에서 다른 프로젝트의 작업이 노출되지 않는다.
- 제목 검색, 상태 필터, 페이징이 함께 동작한다.
- 동일 버전으로 두 번 수정하면 두 번째 요청이 `409 Conflict`가 된다.
- 중복 멤버 추가가 거절된다.

### 테스트 구성 권장

- 도메인/서비스 규칙: 단위 테스트
- 권한, 트랜잭션, JPA 쿼리, 동시성: 통합 테스트
- 주요 API와 오류 응답: `MockMvc` 테스트

---

## 12. 구현 우선순위와 일정

### 1단계 — 실행 가능한 골격

- Spring Boot 3.3.x 프로젝트 생성
- Java 17, Gradle Wrapper, H2, JPA, Validation, Springdoc 설정
- `./gradlew bootRun`과 `./gradlew test` 확인

### 2단계 — 도메인과 기본 CRUD

- 엔티티, enum, 관계, 유니크 제약 정의
- 요청·응답 DTO와 validation
- 사용자 → 프로젝트/멤버십 → 작업 순서로 구현

### 3단계 — 평가 핵심 규칙

- 공통 요청자 식별
- 서비스 계층 권한 검사
- 마지막 OWNER 보호
- 프로젝트별 데이터 격리
- 담당자 검증
- 낙관적 락 및 `409 Conflict`

### 4단계 — 검색과 문서화

- 제목 검색, 상태 필터, 페이징
- Swagger 설명 및 예시
- 실행 즉시 확인할 초기 데이터 또는 명확한 API 호출 순서

### 5단계 — 검증과 제출 준비

- 핵심 테스트 실행
- 새 디렉터리에서 클론했다고 가정한 실행 점검
- README 체크리스트 확인
- 민감 정보와 불필요한 생성 파일 확인
- 공개 GitHub 저장소 링크와 기본 브랜치 확인

### 시간이 부족할 때 줄일 것

1. React 화면
2. 부가 기술 및 CI
3. 과도한 추상화와 리팩터링
4. 부가 필드와 편의 API

권한·데이터 격리·동시성·실행 가능성·README는 줄이면 안 된다.

---

## 13. README에 반드시 설명할 설계 판단

- 인증을 구현하지 않고 `X-Requester-Id`를 사용한 이유
- `ProjectMember`를 독립 엔티티로 둔 이유
- 작업 상태 종류를 그렇게 정한 이유
- 검색의 대상 필드와 매칭 방식
- 페이징 기본값과 정렬 기준
- 담당자 미지정 및 멤버 제거 시 처리 정책
- 역할 변경 범위와 마지막 OWNER 보호 방식
- 낙관적 락을 선택한 이유와 충돌 응답 방식
- 엔티티 대신 DTO를 노출한 이유
- 추가 기술을 사용했거나 사용하지 않은 이유
- 미완성 사항과 개선 계획

면접에서는 다음 질문에 자신의 말로 답할 수 있어야 한다.

- 왜 이 테이블 구조를 선택했는가?
- 권한 검사는 어디에서, 어떤 순서로 수행되는가?
- 다른 프로젝트 데이터가 섞이지 않음을 어떻게 보장하는가?
- `@Version`만 붙이는 것과 요청 버전을 받는 것은 무엇이 다른가?
- 마지막 OWNER 규칙은 동시 요청에서도 안전한가?
- 다른 선택지는 무엇이었고 왜 사용하지 않았는가?

---

## 14. 멀티테넌시 확장 답변 초안

여러 회사가 서비스를 함께 사용하고 회사 간 데이터를 완전히 분리해야 한다면 `Tenant` 또는 `Organization` 도메인을 추가하고, 사용자·프로젝트 등 회사 소유 데이터에 `tenant_id`를 둔다.

필요한 변경:

- 요청자의 인증 정보에서 `tenantId`를 식별
- 모든 조회·수정·삭제 쿼리에 `tenantId` 조건 강제
- 주요 유니크 제약을 tenant 범위로 변경
  - 예: 전역 이메일 정책이 아니라면 `(tenant_id, email)`
- 프로젝트 및 멤버 추가 시 동일 tenant 여부 검증
- 로깅과 감사 기록에 tenant 포함
- tenant 간 데이터 접근 통합 테스트 추가
- 캐시 키, 파일 저장 경로, 메시지에도 tenant 경계 적용

격리 수준 선택지:

1. **공유 DB·공유 스키마 + `tenant_id`**: 비용과 운영 복잡도가 낮아 초기 단계에 적합하지만 모든 쿼리에 경계 적용이 필수
2. **공유 DB·tenant별 스키마**: 격리가 강화되지만 마이그레이션과 운영이 복잡
3. **tenant별 DB**: 가장 강한 격리와 백업 독립성을 제공하지만 비용과 운영 부담이 큼

이 과제 규모에서는 우선 공유 스키마의 `tenant_id` 방식을 선택하고, 민감도와 고객 규모가 커지면 스키마 또는 DB 분리를 검토한다고 설명하면 현실적이다.

---

## 15. 최종 제출 체크리스트

### 코드

- [ ] Java 17 / Spring Boot 3.3.x / Gradle 8.x 조건 충족
- [ ] Gradle Wrapper가 저장소에 포함됨
- [ ] `./gradlew bootRun` 한 번으로 실행됨
- [ ] H2 인메모리 DB로 별도 설치 없이 동작함
- [ ] 사용자·프로젝트·멤버·작업 필수 API 구현
- [ ] 역할별 권한 규칙 구현
- [ ] 비멤버 및 프로젝트 간 데이터 노출 차단
- [ ] 마지막 OWNER 보호
- [ ] 작업 검색·상태 필터·페이징 구현
- [ ] 작업 동시 수정 충돌을 `409`로 처리
- [ ] Swagger UI에서 API 확인 가능
- [ ] 핵심 규칙 테스트 통과

### README

- [ ] 빌드 및 실행 방법
- [ ] Swagger UI 접속 URL
- [ ] 실행 직후 검증 절차 또는 초기 데이터 계정/ID
- [ ] API 명세 요약과 요청·응답 예시
- [ ] 주요 설계 결정과 이유
- [ ] 사용 기술과 선택 이유
- [ ] 멀티테넌시 확장 방안
- [ ] 미완성 항목 및 향후 계획

### 저장소 및 제출

- [ ] API 키, 비밀번호, 개인 정보가 없음
- [ ] `build/`, IDE 설정 등 불필요한 파일이 제외됨
- [ ] 저장소가 공개 상태임
- [ ] GitHub에서 README가 정상 표시됨
- [ ] 새 환경에서 실행 절차를 최종 확인함
- [ ] 2026년 8월 26일 23:59 이전 안내 메일에 저장소 링크 회신

---

## 16. 가장 중요한 제출 전략

완벽하게 많은 기능을 만드는 것보다, 작은 범위를 안정적으로 완성하고 설계 근거를 명확히 보여 주는 편이 유리하다.

최소 제출 기준은 다음과 같다.

> **바로 실행되고, Swagger로 확인할 수 있으며, 프로젝트별 권한과 데이터 경계가 지켜지고, 동시 수정 충돌이 테스트로 증명되며, README에서 모든 판단을 설명할 수 있는 백엔드**

React는 위 기준을 모두 만족하고 시간이 남을 때만 진행한다.
